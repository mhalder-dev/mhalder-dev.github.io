# mhalder-dev.github.io

My personal portfolio → **https://mhalder-dev.github.io**

Single-page site, plain HTML + CSS, no build step.

## Structure
- `index.html` — the whole site (inline CSS).
- `assets/profile.jpg` — hero photo (optimized).

## Editing
Open `index.html` in a browser to preview; edit and push — GitHub Pages redeploys automatically. Keep the photo web-sized (≈700px wide) so the page stays fast.
